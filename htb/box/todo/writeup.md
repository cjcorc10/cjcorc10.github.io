# todo

In this lab we are given another web app directory for static analysis and an ip address for dynamic analysis. The web app is taking objects and placing them in a todo list for the user.

# dynamic analysis

This lab includes a hint in the html source code, by including a note of what not to use. We see that the page is using fetch to refresh the page every 500 ms.  
